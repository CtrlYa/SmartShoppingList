<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.smartshoppinglist">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/shopping_list"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmartShoppingList">
        <activity
            android:name="ru.mperika.smartshoppinglist.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.SmartShoppingList.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>